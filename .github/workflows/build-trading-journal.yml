name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  GOPRIVATE: github.com/trogers1052/*

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25.0'
      - name: Resolve trading-testkit from GitHub
        run: |
          sed -i '/replace.*trading-testkit/d' go.mod
          go get github.com/trogers1052/trading-testkit@v0.2.0
          go mod tidy
      - run: go vet ./...

  unit-test:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25.0'
      - name: Resolve trading-testkit from GitHub
        run: |
          sed -i '/replace.*trading-testkit/d' go.mod
          go get github.com/trogers1052/trading-testkit@v0.2.0
          go mod tidy
      - run: go test -v -race -short -cover ./...

  integration-test:
    name: Integration Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25.0'
      - name: Resolve trading-testkit from GitHub
        run: |
          sed -i '/replace.*trading-testkit/d' go.mod
          go get github.com/trogers1052/trading-testkit@v0.2.0
          go mod tidy
      - name: Run integration tests
        run: go test -v -race -run 'Integration' -timeout 300s ./...

  build:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    needs: [lint, unit-test, integration-test]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25.0'
      - name: Resolve trading-testkit from GitHub
        run: |
          sed -i '/replace.*trading-testkit/d' go.mod
          go get github.com/trogers1052/trading-testkit@v0.2.0
          go mod tidy
      - name: Build Linux ARM64 binary
        env:
          GOOS: linux
          GOARCH: arm64
          CGO_ENABLED: '0'
        run: |
          mkdir -p dist
          go build -trimpath -ldflags="-s -w" -o dist/trading-journal ./cmd/journal
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          platforms: linux/arm64
          push: true
          tags: |
            ghcr.io/trogers1052/trading-journal:latest
            ghcr.io/trogers1052/trading-journal:${{ github.sha }}
